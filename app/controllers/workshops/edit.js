import Ember from 'ember';

// Really not clear why we need this given that we don't have something similar
// for the Speakers.Edit controller, which is generated...
export default Ember.Controller.extend({
	speakers: {}
});
